{% extends 'base-edit-classe.html.twig' %}

{% block title %}Classes{% endblock %}

{% block body %}
    <h1>Classes</h1>
    <table class="table">
        <tbody>
            <tr class="table_title" id="table_title">
            </tr>
            <tr>
                
            </tr>
        </tbody>
    </table>
        <table class="table">
            <thead>
                <tr class="table_title" id="table_title">
                    <th class="table_id">Id</th>
                    <th>Classe_libelle</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th>{{ class.id }}</th>
                    <th>{{ class.classeLibelle }}</th>
                </tr>
            </tbody>
        </table>
    <a href="{{ path('classes_index') }}" >back to list</a>
    <a href="{{ path('classes_edit', {'id': class.id}) }}" >edit</a>
    {{ include('classes/_delete_form.html.twig') }}
{% endblock %}
